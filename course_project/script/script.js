document.addEventListener('DOMContentLoaded', () => {
  const button = document.getElementById('open__item');
  button.addEventListener('click', () => {
    location.href = './page/item.html';
  });
  
});
